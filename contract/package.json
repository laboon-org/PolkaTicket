{
  "name": "contract",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "truffle compile",
    "test": "truffle test",
    "test-mandala": "truffle test --network mandala",
    "test-mandala-pub": "truffle test --network mandalaPublicDev",
    "deploy": "truffle migrate",
    "deploy-mandala": "truffle migrate --network mandala",
    "deploy-mandala-pub": "truffle migrate --network mandalaPublicDev"
  },
  "dependencies": {
    "@acala-network/chopsticks": "^0.6.4",
    "truffle": "^5.9.0"
  },
  "devDependencies": {
    "@truffle/hdwallet-provider": "^2.1.11"
  }
}
